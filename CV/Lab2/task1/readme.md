# Task 1 - Chest X-Ray enhancement

Place one X-ray image in `data/`, then run:

```powershell
python xray_enhancement.py --image data/sample_xray.png

If `--image` is omitted, the script uses the first supported image in `data/`. Results are written to `output/`.
